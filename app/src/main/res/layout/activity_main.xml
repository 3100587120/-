<EditText
    android:id="@+id/etMessage"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1"
    android:hint="输入消息..."
    android:maxLines="3"
    android:textSize="16sp"
    android:padding="12dp"
    android:background="#FFFFFF" />   <!-- 直接白色背景 -->
